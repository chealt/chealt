name: Test - Unit

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/actions/install
    - name: Test
      run: yarn test:unit
