---
interface Props {
  icon: string;
  title: string;
  description: string;
}

const { icon, title, description } = Astro.props;
---

<article class="activity-astro-card">
  <h3>{icon} {title}</h3>
  <p>{description}</p>
</article>
